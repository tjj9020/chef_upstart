source "https://supermarket.getchef.com"

metadata

branch="master"

cookbook "gbg_sudo", git: "https://github.com/genebygene/chef-repo.git", rel: "gbg_sudo", branch: "release"
cookbook "gbg_yum_repo", git: "https://github.com/genebygene/chef-repo.git", rel: "gbg_yum_repo", branch: "#{branch}"
cookbook 'rvm', github: 'fnichol/chef-rvm'
